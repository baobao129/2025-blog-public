I will implement the requested changes to the blog project, focusing on adding popup notifications, custom article covers, and homepage pagination.

### 1. **Popup Notifications (Requirement 1)**
I will create a reusable `ResultModal` component to provide explicit feedback (success/failure) for key actions, replacing or augmenting the existing toast notifications.
*   **Create `src/components/ResultModal.vue`:** A modal dialog that displays a title, message, and type (success/error).
*   **Modify `src/views/Editor.vue`:**
    *   Integrate `ResultModal` to show "Upload Success/Fail" and "Publish Success/Fail" messages.
    *   Ensure detailed error messages are displayed in the modal when operations fail.

### 2. **Custom Article Cover (Requirement 2)**
I will add support for Frontmatter (metadata) to store and retrieve custom cover images for articles.
*   **Create `src/utils/frontmatter.js`:** A utility to parse and stringify YAML-style frontmatter (e.g., `--- \n title: Hello \n cover: url \n ---`) from Markdown files.
*   **Modify `src/views/Editor.vue`:**
    *   Add a "Cover Image URL" input field in the settings sidebar.
    *   Update the `loadPost` and `handleSavePost` logic to read/write the `cover` field in the file's frontmatter.
*   **Modify `src/views/Home.vue`:**
    *   Update the article card to display the custom `cover` image if defined; otherwise, fall back to the existing random gradient.

### 3. **Homepage Pagination (Requirement 3)**
I will implement client-side pagination to limit the number of articles displayed on the homepage.
*   **Modify `src/views/Home.vue`:**
    *   Fetch the full list of article filenames first.
    *   Implement pagination logic (e.g., 6 articles per page) using `currentPage` and `pageSize` states.
    *   Fetch the **content** (to get the cover image and summary) *only* for the articles on the current page to optimize performance.
    *   Add pagination controls (Previous/Next/Page Numbers) at the bottom of the list.

### Plan Execution Order
1.  Create `frontmatter.js` utility.
2.  Create `ResultModal.vue` component.
3.  Update `Editor.vue` (Notifications & Cover Image support).
4.  Update `Home.vue` (Pagination & Cover Image display).
