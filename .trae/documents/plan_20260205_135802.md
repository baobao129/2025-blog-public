I will fix the layout issues, enhance the homepage, and add image upload functionality.

**1. Fix Editor Layout (Remove Outer Scrollbar)**
*   **Goal**: Ensure the Editor page is strictly full-height without global scrollbars.
*   **Action**:
    *   Update `src/router/index.js` to add `meta: { layout: 'fullscreen' }` to `/editor` routes.
    *   Update `src/App.vue` to check `route.meta.layout`. If it is `'fullscreen'`, do *not* render the global `<nav>` and `<footer>` and remove the global padding wrapper. This gives `Editor.vue` full control over the viewport.

**2. Enable Intelligent Autocomplete**
*   **Goal**: Enable "smart fill" / autocomplete in Monaco Editor.
*   **Action**:
    *   In `src/components/MonacoEditor.vue`, change `suggest: { showWords: false }` to `true` (or remove the line to use default `true`).
    *   Ensure `quickSuggestions` is enabled.

**3. Fix Toast Position**
*   **Goal**: Ensure toast appears at the top center.
*   **Action**:
    *   Verify `src/App.vue` uses `<Toaster position="top-center" />`. It appears correct, but I will double-check and potentially increase `z-index` via `toastOptions` if it's being obscured.

**4. Beautify Homepage**
*   **Goal**: Improve `src/views/Home.vue` visual appeal.
*   **Action**:
    *   Enhance the "Zen Minimalist" design.
    *   Add visual flair to article cards: subtle gradients, better typography, and "random" colored accents or patterns for posts without images.
    *   Improve the Hero section with a more refined layout.

**5. Image Upload Integration**
*   **Goal**: Allow uploading images in the editor to `images/` directory in GitHub.
*   **Action**:
    *   **Backend (GitHub Client)**: `src/utils/github-client.js` already has `putFile`. I will use it directly.
    *   **Frontend (Editor)**:
        *   In `src/views/Editor.vue`, add an "Insert Image" button to the toolbar.
        *   Implement `handleImageUpload` function:
            1.  Trigger file input.
            2.  Read file as Base64.
            3.  Generate unique filename (timestamp + original name).
            4.  Call `putFile` to upload to `images/`.
            5.  Get the raw URL (e.g., `https://raw.githubusercontent.com/...` or relative path `/images/...`).
            6.  Insert `![alt](url)` into the editor using `monacoRef.value.insertText`.