---
cover: 
---
# 浮动

## 应用场景

1. 文字环绕
2. 横向排列

## 浮动的基本特点

修改float属性值为：

- left：左浮动，元素靠左靠上
- right：右浮动，元素靠上靠右

默认值为none

1. 当一个元素浮动后，元素必定为块盒（更改display属性为block）
2. 浮动元素的包含块，和常规流一样，为父元素的内容盒

## 盒子尺寸

1. 宽度为auto时，适应内容宽度，内容为空时为0.
2. 高度为auto时，与常规流一直致，适应内容高度，内容为空时为0。
3. margin为auto时，为0。
4. 边框、内边距、百分比设置与常规流一样。
