# 盒模型
## 盒模型分类
- 行盒：display为inline的元素
- 块盒：display为block的元素
- 区别：行盒在页面中不换行，块盒独占一行
## 浏览器默认样式
- 默认块盒：容器元素h1-h6、p
- 默认行盒：span、a、img、video、audio
## 盒子的组成部分
无论是行盒还是块和都是由下面的几个元素组成，从内到外分别是:
1、内容 content
2、填充（内边距） padding
3、边框 border
4、外边距 margin